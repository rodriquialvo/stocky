import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: 'Cormorant Garamond, sans-serif', // Fuente para títulos
    body: 'Cormorant Garamond, sans-serif',   // Fuente para el cuerpo del texto              // Fuente para el cuerpo del texto
  },
});

export default theme;